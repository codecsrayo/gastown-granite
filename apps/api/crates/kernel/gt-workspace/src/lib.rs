//! `gt-workspace` — town root, paths, `FindFromCwd` (ver `docs/02-tree.md`).
//!
//! Skeleton (Paso 0): resolución del town root subiendo desde el cwd hasta hallar el marcador
//! del workspace; deriva rutas (`.beads/`, `.events.jsonl`, etc.).

// TODO: find_town_root(cwd) -> Option<PathBuf>; paths del town derivados de la raíz.
