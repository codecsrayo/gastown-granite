[package]
name = "gt-orchestration"
edition.workspace = true
version.workspace = true
rust-version.workspace = true

[dependencies]
gt-events = { path = "../../kernel/gt-events" }
tokio.workspace = true
serde.workspace = true

[dev-dependencies]
# The gate test plays the composition-root role (wires actor + records + replays).
# gt-audit is the log/replay machinery; ulid only seeds unique temp paths. The library
# itself depends ONLY on the kernel (gt-events) — never on another domain.
gt-audit = { path = "../../kernel/gt-audit" }
ulid.workspace = true
