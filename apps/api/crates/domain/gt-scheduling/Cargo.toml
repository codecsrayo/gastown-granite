[package]
name = "gt-scheduling"
edition.workspace = true
version.workspace = true
rust-version.workspace = true

[dependencies]
gt-events = { path = "../../kernel/gt-events" }
gt-beads = { path = "../../kernel/gt-beads" }
tokio.workspace = true
serde.workspace = true
# JSON Schema derives for Command arguments — surfaced by gt-mcp.
schemars.workspace = true

[dev-dependencies]
# El test e2e juega el rol de composition root: cablea DOS dominios (scheduling + agent).
# Son dev-deps (test-only) — la librería NO depende de otro dominio.
gt-agent = { path = "../gt-agent" }
gt-audit = { path = "../../kernel/gt-audit" }
serde_json.workspace = true
